n=1000 (nombre d'exemples)
sig0=10^-1
sigmax=10^-1/2
Evolution de sig et eps : linéaire
nbiter = 60000
gauss = exp((-d/sig)^2/2) (j'ai supprimé le facteur 1/sqrt(2*pi))

Entraînement du vainqueur :

win(t+1)=win(t)+eps*(vector-win(t))

Modification de la fonction d'apprentissage des voisins de la forme:

wi(t+1)=wi(t)+coeff_dist_win_ij*(vector-wi(t))

coeff_dist_win_ij représente le coefficient d'apprentissage en fonction de la distance entre 
le neurone gagnant et le neurone ij
coeff(i,j,vector) représente le coefficient d'apprentissage en fonction de la distance entre 
le vecteur d'entrée et le neurone ij

Problème : non redéploiement de la carte, les points sont concentrés au centre de la carte
