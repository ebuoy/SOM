n=1000 (nombre d'exemples)
sig0=10^-1
sigmax=10^-1/2
Evolution de sig et eps : linéaire
gauss = exp((-d/sig)^2/2)/sqrt(2*pi)
nbiter = 60000

Modification de la fonction d'apprentissage des voisins de la forme:

wi(t+1)=wi(t)+coeff_dist_win_ij*coeff(iwin,jwin,vector)*(vector-wi(t))

coeff_dist_win_ij représente le coefficient d'apprentissage en fonction de la distance entre 
le neurone gagnant et le neurone voisin